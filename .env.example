# Database Configuration
# Copy this file to .env and update with your actual credentials
# NEVER commit the .env file to version control

DB_HOST=localhost
DB_USER=your_database_user
DB_PASS=your_secure_password_here
DB_NAME=rsc_site

# Site Configuration (optional overrides)
# SITE_URL=https://2007rshelp.com
# SITE_NAME=Old School RuneScape Help

# To use environment variables:
# 1. Copy this file to .env
# 2. Update the values with your actual credentials
# 3. Ensure .env is in your .gitignore
# 4. Set up your web server to load environment variables from .env
#    OR use a library like vlucas/phpdotenv to load the .env file
